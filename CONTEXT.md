# 도메인 용어

이 문서는 **용어집**이다. 구현 결정은 담지 않는다 — 그것은 `.scratch/single-to-distributed/`의 결정 티켓에 있다.

용어의 소스 오브 트루스는 `contract/api.json`이다. 계약이 쓰는 이름과 이 문서가 어긋나면 계약이 이긴다.

---

## 콘텐츠 (Content)

평가·큐레이션의 **대상**. 영화, 드라마, 스포츠 경기를 하나의 개념으로 아우른다.

**콘텐츠는 미디어 파일이 아니다.** 이 서비스는 영상을 저장하지도 재생하지도 않는다. 콘텐츠는 외부 API(TMDB, The Sports DB)에서 수집한 **메타데이터**이며, 사용자가 평가하고 목록에 담고 "지금 보고 있다"고 표시하는 대상일 뿐이다.

**콘텐츠 타입**은 `movie`, `tvSeries`, `sport` 셋뿐이며 계약이 고정한다. 타입은 콘텐츠의 속성이지 별개의 개념이 아니다 — 셋은 함께 목록에 오르고 함께 검색된다.

## 태그 (Tag)

콘텐츠에 붙는 분류 표지. 사용자가 아니라 콘텐츠에 속한다. 검색 필터의 축이다.

## 리뷰 (Review)

한 사용자가 한 콘텐츠에 남기는 **평가**. **텍스트와 평점은 분리된 개념이 아니라 리뷰 하나의 두 부분이다** — 계약의 `ReviewDto`가 `text`와 `rating`을 함께 갖는다.

요구사항 문서가 "의견이나 평점을 남길 수 있다"고 표현해 둘로 들리지만, 도메인에는 **리뷰 하나만 있다.** "평점만 남긴다"는 개념은 없다.

## 플레이리스트 (Playlist)

한 사용자가 소유하는 **콘텐츠 묶음**. "보고 싶은 것", "비 오는 날 볼 것" 같은 개인의 기준으로 모은다. 소유자만 편집한다.

## 구독 (Subscription)

**플레이리스트를 대상으로 하는 관계.** 구독하면 그 플레이리스트에 콘텐츠가 추가될 때 알림을 받는다.

## 팔로우 (Follow)

**사용자를 대상으로 하는 관계.** 구독과 혼동하지 않는다 — **구독의 대상은 플레이리스트, 팔로우의 대상은 사람이다.** 둘은 각각 별도의 알림을 만든다.

## 대화 (Conversation)

두 사용자 사이의 **DM 통로**. 두 사람당 하나만 존재한다.

## DM (Direct Message)

대화에 속하는 **메시지 한 건**. 대화 없이 존재할 수 없다.

## 시청 세션 (Watching Session)

한 사용자가 **지금 어떤 콘텐츠를 보고 있는가**를 나타내는 상태.

**시청 기록이 아니다.** 과거에 무엇을 봤는지는 이 개념에 없다. 현재만 있다. 그리고 **한 사용자는 최대 하나의 시청 세션을 갖는다** — 계약이 단건을 반환한다.

이 서비스가 영상을 재생하지 않으므로, 시청 세션은 "재생 중"이 아니라 **사용자가 그 콘텐츠 화면에 머물러 있음**을 뜻한다.

## 시청자 수 (Watcher Count)

한 콘텐츠의 **현재** 시청 세션 개수. 누적이 아니다.

## 알림 (Notification)

사용자에게 전달되는 **사건의 통지**. 여섯 가지 사건에서 발생한다 — 권한 변경, 내 플레이리스트가 구독됨, 구독한 플레이리스트에 콘텐츠 추가, 팔로우한 사용자의 활동, 나를 팔로우, DM 수신.

## 사용자 (User) / 어드민 (Admin)

역할은 `USER`와 `ADMIN` 둘뿐이다. 어드민은 별개의 개념이 아니라 **사용자의 역할**이다.

**잠긴 계정(locked)** 은 삭제된 계정과 다르다. 존재하지만 로그인할 수 없는 상태다.

## 임시 비밀번호 (Temporary Password)

비밀번호를 잊은 사용자가 **한시적으로** 로그인할 수 있게 해주는 값. 본 비밀번호를 대체하지 않고 **함께 유효**하며, 사용자가 비밀번호를 재설정하는 순간 파기된다. 만료 시간이 있다.

## 소셜 계정 연동 (Social Account Link)

외부 제공자(구글, 카카오)의 계정을 이 서비스의 사용자와 **잇는 것**. 연동은 새 사용자를 만드는 것이 아니라 **기존 사용자에게 로그인 경로를 하나 더 붙이는 것**이다.

카카오는 이메일을 제공하지 않으므로 가상 이메일을 부여받는다 — 그 값은 연락 수단이 아니라 **식별자**다.
