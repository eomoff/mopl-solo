# 기여 규칙

혼자 하는 프로젝트지만 실무에서 가장 널리 쓰이는 방식을 그대로 따릅니다.
근거가 되는 결정은 [브랜치 전략과 CI/CD 결정](.scratch/single-to-distributed/issues/12-ci-cd.md)에 있습니다.

## 흐름

```
main에서 브랜치 → 작업 → 로컬 검증 → PR → CI 통과 → squash 머지 → 브랜치 삭제
```

`main`은 보호되어 있어 직접 푸시할 수 없습니다. 필수 상태 검사(`빌드 · 테스트 · 커버리지`)가 통과해야 머지됩니다. 승인은 요구하지 않습니다 — 혼자인데 승인을 요구하면 매번 우회하게 되고, 우회하는 규칙은 없느니만 못합니다.

## 브랜치

`<타입>/<슬러그>` 형식입니다. 슬러그는 영어 소문자와 하이픈으로 짧게 씁니다.

```
feat/user-entity
fix/refresh-token-rotation
docs/contributing-guide
chore/bump-flyway
```

| 타입 | 쓰는 곳 |
| --- | --- |
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 |
| `refactor` | 동작 변화 없는 구조 변경 |
| `test` | 테스트만 추가·수정 |
| `chore` | 빌드·설정·의존성 |
| `ci` | 워크플로 |

GitHub 이슈는 만들지 않습니다. PR이 변경 기록을 대신합니다.

## 커밋

[Conventional Commits](https://www.conventionalcommits.org/ko/v1.0.0/)를 따릅니다.

```
<타입>: <제목>

<본문 — 무엇이 아니라 왜>
```

- 제목은 한 줄로 짧게, 마침표 없이 씁니다
- **본문에는 "왜"를 씁니다.** 무엇을 바꿨는지는 diff가 말해주지만, 왜 그렇게 했는지는 커밋에만 남습니다
- 판단으로 정한 것, 대안을 버린 이유, 나중에 뒤집힐 수 있는 전제를 함께 적습니다
- 한 커밋은 하나의 논리 단위입니다. 되돌릴 수 있는 크기로 유지합니다

**이음매 전환은 전환마다 PR 하나입니다.** 그래야 "전파를 분산으로 바꿈"이 되돌릴 수 있는 단위가 됩니다.

## PR

- 제목은 커밋과 같은 형식입니다
- 본문에 **무엇을 / 왜 / 확인 방법**을 적습니다
- **squash 머지**만 사용하고, 머지 후 브랜치를 삭제합니다

## 리뷰

봇 리뷰어를 쓰지 않으므로, **PR을 올리기 전에 로컬에서 `/code-review`를 돌립니다.** 자동으로 걸리는 게이트가 아니라 습관에 의존하는 방식이라, 빠뜨려도 아무것도 막지 않습니다.

## 스키마 변경

`src/main/resources/db/migration`에 `V<번호>__<설명>.sql`로 추가합니다. **4주차 첫 배포 전까지는 기존 파일을 고쳐 써도 되고, 그 뒤로는 append-only입니다.** 파괴적 변경은 두 단계로 나눕니다 — 먼저 코드가 그 컬럼을 안 쓰게 하고 배포한 뒤, 다음 마이그레이션에서 삭제합니다.

## 커밋 전 확인

```bash
./gradlew build   # 테스트 + 커버리지 게이트(80%)
```

Docker가 떠 있어야 합니다 — 테스트가 Testcontainers로 실제 PostgreSQL을 띄웁니다.
